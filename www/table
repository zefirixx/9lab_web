CREATE TABLE conference_registrations (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    birthyear INT,
    section_name VARCHAR(100),
    need_certificate TINYINT(1),
    participation_form VARCHAR(50)
);
